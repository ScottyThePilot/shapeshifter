[package]
name = "shapeshifter"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "1.25" }
itoa = { version = "1.0.17", optional = true }

[features]
default = ["printable_numbers"]
printable_numbers = ["dep:itoa"]
